---
import { sidebar } from '../data/site'
import type { NavItem } from '../data/site'
import { isActiveLink } from '../lib/isActiveLink'

const currentPath = Astro.url.pathname
---

<aside class="sidebar">
  {sidebar.map((group: NavItem) => (
    <div class="sidebar-group">
      <span class="sidebar-group-title">{group.text}</span>
      {group.items && (
        <ul>
          {group.items.map((item: NavItem) => (
            <li>
              <a href={item.link} class={isActiveLink(currentPath, item.link) ? 'active' : ''}>{item.text}</a>
            </li>
          ))}
        </ul>
      )}
    </div>
  ))}
</aside>

<style>
  .sidebar {
    width: 272px;
    flex-shrink: 0;
    padding: 2rem 1rem 2rem 1.5rem;
    overflow-y: auto;
    position: sticky;
    top: 64px;
    max-height: calc(100vh - 64px);
  }
  .sidebar-group { margin-bottom: 1.5rem; }
  .sidebar-group-title {
    font-family: var(--edoxen-font-mono);
    font-size: 0.78rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--vp-c-text-3);
    margin-bottom: 0.5rem;
    display: block;
  }
  .sidebar-group ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .sidebar-group li { margin: 0; }
  .sidebar-group a {
    display: block;
    padding: 0.35rem 0.75rem;
    border-radius: 6px;
    text-decoration: none;
    color: var(--vp-c-text-2);
    font-size: 0.88rem;
    line-height: 1.4;
    transition: color 0.15s, background 0.15s;
  }
  .sidebar-group a:hover {
    color: var(--vp-c-brand-1);
    background: var(--vp-c-brand-soft);
  }
  .sidebar-group a.active {
    color: var(--vp-c-brand-1);
    font-weight: 600;
    background: var(--vp-c-brand-soft);
  }
  @media (max-width: 1024px) {
    .sidebar {
      display: none;
    }
  }
</style>
